{
  "name": "fingerprint",
  "description": "Returns the fingerprint of a text: the first 12 hex digits of its SHA-256.",
  "inputSchema": {
    "type": "object",
    "properties": {"text": {"type": "string", "description": "The text to fingerprint."}},
    "required": ["text"]
  },
  "command": ["./run.py"]
}
